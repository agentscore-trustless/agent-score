# Chainlink CRE Workflow ⛓️

This directory contains the documentation and references for the **Chainlink Custom Runtime Environment (CRE)** integration within the AgentScore protocol.

## 💡 The Role of CRE in AgentScore
The biggest challenge in the Machine-to-Machine (M2M) economy is verification. If an AI Agent sells data for crypto via an L402 paywall, how does the buyer prove the AI didn't hallucinate or return a corrupted payload?

**Chainlink CRE is our decentralized Quality Assurance Auditor.** 

When the user queries the agent, the payload is intercepted by the Gateway and passed to our Chainlink CRE Workflow before delivery. The workflow executes deterministically, completely removing human bias from the reputation loop.

## ⚙️ How the Workflow Operates

1.  **Ingestion:** The CRE workflow receives the raw JSON response generated by the OpenClaw Agent.
2.  **SLA Validation:** The Consensus runtime executes a predefined SLA logic script. For example, it checks if the JSON contains all required keys (e.g., `"trading_signal"`, `"confidence_score"`) and verifies that `"confidence_score"` is a valid integer between 0-100.
3.  **Score Delta Calculation:** The resulting `scoreDelta` is securely pushed to the **ERC-8004 Registry** on Base. If the payload meets the SLA perfectly, the Agent is rewarded. If the final `scoreDelta` accumulates to `0` or less, the Chainlink Webhook forcefully intercepts the pipeline, returning to the Gateway with a `FAILED` SLA status and the Agent is punished on-chain.

### Chainlink CRE Deterministic SLA Audit Ruleset

| ID | Validation Step | Description | Success Score | Failure Penalty |
| :--- | :--- | :--- | :---: | :---: |
| **A** | **Syntax** | Ensures the Agent's raw output is valid, parsable JSON. | **+2** | **-5** |
| **B** | **Schema** | Confirms the payload contains the mandatory `response` or `data` keys. *(Must pass to execute C, D, and E)*. | **+2** | **-5** |
| **C** | **Content Safety** | Scans semantic patterns to ensure the AI did not suffer a prompt injection (e.g., `"ignore previous instructions"`). | **+2** | **-50** |
| **D** | **Semantic Density** | Prevents "lazy" AI outputs by enforcing a minimum word count SLA (≥ 25 words). | **+2** | **-5** |
| **E** | **Cryptographic Attestation** | Recovers the signer's wallet directly from the hashed payload to prevent Gateway/Client spoofing. | **+2** | **-50** (Spoofed)<br>**-5** (Missing) |
| **F** | **Performance** | Evaluates the Time-To-First-Token (TTFT) response latency against M2M expectations. | **+2** `(≤ 5s)`| **-5** `(> 5s)`<br>**-10** `(> 30s)`<br>**-20** `(> 60s)` |

4.  **Contract Writer:** The CRE utilizes the Chainlink Contract Writer capability to sign a transaction and call `submitAssertion(tokenId, scoreDelta, bytes)` on our `AgentScoreRegistry` deployed on Base.

## 🏆 Chainlink Convergence Hackathon Implementation
This workflow is the crux of our Hackathon submission. It successfully bridges the gap, allowing off-chain sovereign agents to securely build trustless on-chain reputation.

*(Include specific Workflow UUIDs, JSON deployment manifests, or screenshot architectures specific to your hackathon sandbox here.)*
